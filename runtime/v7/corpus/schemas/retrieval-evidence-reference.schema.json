{
  "$schemaDialect": "rain-push-corpus/mini-schema/1",
  "$id": "retrieval-evidence-reference",
  "title": "Retrieval evidence reference",
  "description": "A pointer used at generation time to justify a produced line by REFERENCE, not by copying. It cites which public derived event(s) and which specific message hashes were retrieved as evidence, plus the declared usage (behavior vs wording vs mechanics). It never embeds verbatim text. The cross-record validator checks that the declared usage is actually permitted by the referenced event's capabilities.",
  "type": "object",
  "required": ["recordFormatVersion", "id", "references"],
  "additionalProperties": false,
  "properties": {
    "recordFormatVersion": { "type": "integer", "const": 1 },
    "id":       { "type": "string", "pattern": "^ret_[a-z0-9_]+$" },
    "producedForEventId": { "type": "string", "pattern": "^evt_[a-z0-9_]+$" },
    "references": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/reference" }
    }
  },
  "$defs": {
    "reference": {
      "type": "object",
      "required": ["eventId", "usage"],
      "additionalProperties": false,
      "properties": {
        "eventId": { "type": "string", "pattern": "^evt_[a-z0-9_]+$" },
        "usage":   { "type": "string", "enum": ["behavior", "wording", "mechanics"] },
        "messageHashes": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
        }
      }
    }
  }
}
