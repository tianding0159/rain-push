node_modules/
.wrangler/
.dev.vars
*.log

# 素材与设计中间产物,不入库(运行时用的图标已内嵌进 src/icon_*.mjs)
assets/

# 备份打包产物,不入库
rain-push-full.bundle
rain-push-worktree.tar.gz

# v7 引擎 zip 的解压工作区(deliverable 是根目录的 zip + runtime/v7/engine/ reviewable 副本,
# 解压出来的 handoff/ 只是临时工作态,不入库)
handoff/

# P0-0A: deterministic engine archive is regenerable; commit the manifest, not the zip
runtime/v7/dist/

# P0-value: private corpus + private character-signal annotations + raw generated
# candidates that embed private retrieval text + rater identities + unredacted failure
# examples. NEVER committed. Loaded at runtime via RAIN_PUSH_PRIVATE_CORPUS or these paths.
# CI uses runtime/v7/value-proof/fixtures/synthetic/ only. Literal patterns (gitignore does
# not expand env vars); keep in sync with runtime/v7/value-proof/README.md privacy boundary.
runtime/v7/value-proof/private/
*.private.json
*.private.jsonl
